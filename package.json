{
  "name": "skugal-assignment",
  "version": "0.0.1",
  "description": "Skugal Assignment",  
  "license": "",
  "author": "Asha Soni",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "less": "^4.1.1",
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {}
}
